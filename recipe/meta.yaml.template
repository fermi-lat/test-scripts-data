{% set name = "fermitools-test-scripts-data" %}
{% set version = "0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  path: ../test-scripts-data

build:
  number: 0
  noarch: generic